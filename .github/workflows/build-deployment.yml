name: bomach CI/CD

on: 
  push:
    branches:
      - "main"
      - "master"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Push Repo
      uses: actions/checkout@v3
      - name: Print out the following
      run: echo "Hello world"
