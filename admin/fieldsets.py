fieldsets_admin = (
      ('Realestate Details', {
          'fields': ('name',)
      }),
      ('Quantity Price', {
          'fields': ('total_amount', 'amount_deposited', 'unit_price')
      }),

      ('Extra Fees', {
          'fields': ('legal_fee', 'survey_plan', 'development_fee')
      }),
   )